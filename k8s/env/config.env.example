# Non-sensitive configuration for Flink app (ConfigMap)
# Copy to k8s/env/config.env and edit values.

PAIMON_WAREHOUSE=s3://paimon/
S3_ENDPOINT=https://minio.minio-tenant.svc.cluster.local
S3_PATH_STYLE=true
PAIMON_CATALOG=paimon_catalog
PAIMON_DATABASE=demo
PIPELINE_NAME=pyflink-paimon-page-views-per-minute
CHECKPOINT_INTERVAL=10 s
# Optional: override SQL search paths (image first, then mounted code)
# SQL_DIRS=/opt/flink/sql:/opt/flink/usrcode/sql
